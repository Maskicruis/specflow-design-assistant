# Copy this file to .env on EACH computer. Never commit the .env file.
# Existing operating-system environment variables take precedence.
DEEPSEEK_API_KEY=

# Optional; relative paths are resolved from the project directory.
# SPEC_DATA_DIR=data
# SPEC_SOURCES_DIR=规范文件
