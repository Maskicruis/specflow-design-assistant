# 规序 SpecFlow 项目介绍

## 1. 项目要解决什么问题

设计工作往往从一个具体问题开始，例如“站址选择需要考虑什么”“消防车道需要核对哪些条文”。资料却分散在不同规范、不同版本和不同页码中，表格、脚注、图示还可能与正文分离。

本项目把这条工作链连接起来：**设计场景 → 找到规范证据 → 整理参考回答 → 打开原文复核 → 留存设计依据**。它帮助设计人员查找和理解规范，不代替专业判断，不直接审批方案，也不自动出施工图。

v1.1 已删除图谱页面、关系图交互和图谱导出接口。保留设计流程入口，是因为“当前在做哪一步设计”能直接帮助缩小查找范围。这里的流程驱动指设计场景组织和可恢复的文档处理步骤，没有引入复杂的 BPM 审批引擎。

## 2. 如何构建：先建立可信资料层，再接入模型

我将项目拆成四层。前端负责设计人员的操作，接口层组织任务，解析和检索模块处理规范，SQLite 保存资料结构和用户记录。各层直接通过清晰的 Python 函数和 HTTP 接口协作，当前不依赖微服务、外部数据库或云端工作流平台，便于在普通电脑上安装和演示。

| 层次 | 实现 | 选择原因 |
|---|---|---|
| 操作界面 | 原生 HTML、CSS、JavaScript | 中文工作台，无前端编译和外部 CDN 依赖 |
| 本地服务 | Python、FastAPI、Uvicorn | 统一管理上传、查询、模型连接和原文访问 |
| 资料理解 | PyMuPDF、python-docx、视觉语言模型 | 对不同文档采用适合其结构的解析方式 |
| 资料与记录 | SQLite，WAL 事务模式 | 文件化部署，保存文档、页面、结构块、证据、任务和收藏 |
| 检索与回答 | 中文 BM25、流程/主题规则、LLM | 先找实际来源，再要求模型基于来源整理回答 |

没有重新训练一个大模型。规范内容存放在本地资料库中，提问时只把检索出的有关内容交给配置的模型，这就是本项目采用的 RAG（检索增强生成）。替换或增加规范通常只需要重新导入，无须训练模型。

## 3. 多模态内容怎样处理

导入前先检查文件类型并计算 SHA-256，完全相同的文件不会重复建立文档身份。原文件保持不变，结构化结果另行保存。

**可编辑 PDF**：直接读取原生文字对象、字体、坐标、阅读顺序和矢量表格结构。保留表格行列、原始页面及结构区域；不先把所有页面转成图片识字。图像为主或没有可靠原生文字的页面进入视觉理解队列。

**扫描 PDF 和图片**：把页面渲染为图像交给视觉语言模型，要求同时理解版式、条文、图表、脚注和单位，返回带位置及不确定标记的结构化 JSON。应用不安装或调用 Tesseract、PaddleOCR 等 OCR 引擎，也没有 OCR 文本中间层。模型自身如何完成视觉识读取决于模型提供方，应用层不宣称视觉解析绝对无误。

**Word DOCX**：读取 OOXML 中的段落、样式、字形属性、表格和合并信息，并保留内嵌位图供视觉模型理解。Word 中的“页”取决于字体和排版环境，因此当前用正文块定位，不编造 PDF 式物理页码。复杂分页、公式对象、SmartArt 和所有浮动对象尚未做完整语义还原，精确排版要打开原始 Word 文件核对。旧 `.doc` 需要先另存为 `.docx`。

原生解析提供可直接读取的结构，视觉模型处理扫描和图像语义，原文查看器保留最终核验入口。三者共同工作，避免只保存一份失去版式的纯文本。

## 4. 如何把资料组织成设计可用的依据

解析结果分为文档、页面或正文块、结构块和检索证据。证据保留文档 ID、条文号、页码范围、类型、提取方法和来源链接。条文边界识别会连接相邻页续文，并尽量区分正文与条文说明；复杂续表仍按页及表题保留，不假设已经完成所有跨页表格合并。

随后根据可编辑关键词规则，给证据附加“站址与规划”“消防与安全”等设计阶段和主题标签。同一证据可以涉及多个阶段。规则写在 `app/ontology.py`，这个文件名保留只是为了维持现有分类模块，程序已经没有图谱展示或图数据库。

这类标签是查找入口，不是专家审定的合规规则。例如某条材料含有“消防”并不能证明它适用于所有消防设计；仍须结合工程类型、规范版本和适用条件判断。

## 5. 用户提问后发生什么

1. 接收设计问题、所选阶段及可选文件范围。
2. 将中文拆成二字、三字片段，识别英文、数字和条文编号，并应用少量工程同义词。
3. 使用 BM25 找到词项相关的证据，再结合流程标签、主题和工程对象调整排序，降低目录等内容的优先级。
4. 为回答选取最多七条来源；未复核表格中的具体数值先从模型上下文中隐藏，并提示需要核验原页。
5. 模型根据这些来源生成参考回答、待确认条件和引用编号。程序检查引用是否属于本次提供的来源集合。
6. 无依据时不生成结论；模型不可用或返回无效引用时，明确降级为原文证据模式。
7. 回答、来源和查询记录保存在本机，可回看、收藏和导出。

“即时查找”主要指已经建立索引后的本地检索。第一次解析扫描文件和生成模型回答需要等待网络与模型，不承诺所有操作瞬时完成。当前检索是词法检索，尚未实现神经语义向量召回和独立重排；同义表达覆盖不足是已知限制。

## 6. 如何控制质量与失败

处理队列记录每页的状态。成功页面保存后可继续使用，失败页保留重试入口；视觉批次最多同时发出三个请求，允许停止和续跑。模型输出被截断、结构不合法或明确不可辨认时，不把相应未知内容当成可靠证据。

机器解析完成与人工复核是独立状态。界面保留报批稿提示、原页、表格和复核标记。引用校验只能验证来源编号存在，无法证明每句话被条文充分支持，也不能识别所有遗漏、数值误读和规范版本冲突。这里仍需要设计人员的责任判断。

自动化测试使用合成 PDF、DOCX 和模拟模型输出，覆盖文件导入、表格合并、图像保留、续文、拒答、错误引用、未复核数值保护、迁移和备份。测试不需要真实 API 密钥，也不会上传用户规范。测试通过并不等于工程答案准确率已经完成正式评测。

## 7. 为什么这样部署和更新

GitHub 管理程序代码和文档，每台电脑保留自己的规范、数据库和密钥。原文件使用相对资料根目录的定位方式；旧版绝对路径迁移前会核验 SHA-256，避免同名文件被错误替换。

新电脑克隆代码、创建虚拟环境、配置模型，然后导入文件或恢复私人资料备份。更新脚本先检查源码是否被本地修改、停止空闲服务并备份资料，再拉取代码；不使用强制覆盖。它不是数据库多端同步工具，两台电脑的收藏和后续导入不会自动合并。

当前仍是单机版本。团队集中使用需要进一步建设登录、权限、资料隔离、上传安全检查、服务监控和备份策略，不能简单把现有端口开放给所有人。

## 8. 后续建设建议

优先补齐规范版本台账、工程条件录入、复杂表格专项复核和真实设计题集。然后在评测结果支持的情况下引入向量召回与重排，提高不同问法下的命中率。进一步可形成按设计阶段输出的核查清单，但每一项仍需保留出处、条件和人工确认。

这些工作直接提高“基于规范辅助设计”的实用性，不依赖重新加入图谱展示。
